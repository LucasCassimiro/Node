const assert = require('assert')

const SENHA = 'Lucas@123'
describe.only('UserHelper test suite', function (){
    it('Deve gerar um hash a partir de uma senha', async () => {
        const result = ''
        assert.ok(result.length>10)
    })
})